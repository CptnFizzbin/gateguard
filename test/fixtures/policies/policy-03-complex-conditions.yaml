version: "1.0.0"
name: "Status-Based Policy"
description: "Complex conditions based on article status"

rules:
  - [allow, Read, Article]
  - [allow, Create, Article]
  - [allow, Update, Article, {
      $and: [
        { owner_id: 1 },
        { status: { $ne: archived } }
      ]
    }]
  - [allow, Delete, Article, { status: { $ne: archived } }]
  - [deny, Delete, Article, { status: archived }]
